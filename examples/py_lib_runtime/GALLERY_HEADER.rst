Examples
========

These are complete live programs built only on the public ``py_lib_runtime`` API.
They use real dependencies, credentials, and external services when a workflow needs them.
Required CI imports and type-checks them but does not run live workflows.
